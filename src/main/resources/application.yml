spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        default_schema: sch_techindustry

  thymeleaf:
     cache: false

api:
  security:
    token:
      secret: ${JWT_SECRET}